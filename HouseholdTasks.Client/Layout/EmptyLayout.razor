@* Bare layout with no header, nav, or login UI — for standalone display pages like the
   kiosk screen, which are meant to be embedded (e.g. in a Home Assistant dashboard) with
   nothing but the content itself. *@
@inherits LayoutComponentBase

@Body
